#include "main.h"
#include <stdio.h>
/**
 * _abs - computes the absolute value of an integer
 *@x: this is variabel for the condichen
 * Return: 0
*/
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
		return (-x);
}
